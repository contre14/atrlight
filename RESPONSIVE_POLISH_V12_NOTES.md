# Responsive Polish V12 — Logo cleanup

- Rebuilt `assets/atr-logo-optimized.png` on a larger transparent canvas so the cyan star/glow fades naturally instead of showing a hard rectangular cutoff.
- Forced both header and footer logos to preserve their natural aspect ratio.
- Fixed the footer logo distortion caused by its HTML height attribute competing with a CSS width-only rule.
- No content, layout, scroll-snap behavior, or other approved styling was changed.
