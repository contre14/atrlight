# Scale statistic text spacing

This pass fixes the intended spacing inside the statistic values themselves.

- `40–50` → `40 – 50`
- `≈25` → `≈ 25`
- Used non-breaking spaces in HTML so the values keep their spacing and stay on one line.
- Restored the original approved card layout, font sizes, and positioning.
- Applied identically to both light and dark themes.
